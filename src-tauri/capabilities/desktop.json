{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop:default",
  "description": "Default permissions for the desktop platform.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "shell:allow-open",
    "opener:default"
  ]
}