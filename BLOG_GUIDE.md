# Hugo博客集成指南

## 🎉 集成完成！

你的Hugo博客系统已成功集成到Nuxt项目中。

## 📁 项目结构

```
util/
├── blog/                    # Hugo博客源文件
│   ├── hugo.toml           # Hugo配置
│   ├── content/            # 博客内容
│   └── layouts/            # 自定义布局
├── public/blog/            # Hugo生成的静态文件
├── src/pages/blog.vue      # Nuxt博客入口页
└── package.json            # 新增的博客构建脚本
```

## 🚀 快速开始

### 创建新文章
```bash
npm run blog:new
```

### 开发模式（同时运行Nuxt和Hugo）
```bash
npm run dev:blog
```

### 构建博客
```bash
npm run blog:build
```

### 完整构建（博客+Nuxt）
```bash
npm run build
```

## 🌐 访问路径

- **博客首页**：`http://localhost:3000/blog/`
- **示例文章**：`http://localhost:3000/blog/posts/welcome-to-our-blog/`
- **RSS Feed**：`http://localhost:3000/blog/feed.xml`

## ✨ 特性

1. **无缝集成**：Hugo生成的静态文件完美集成到Nuxt项目
2. **SEO友好**：完整的RSS Feed、Sitemap支持
3. **响应式设计**：适配移动端和桌面端
4. **开发便捷**：支持热重载、并行开发
5. **生产就绪**：优化的构建流程

## 📝 写作指南

### 文章格式
```markdown
---
title: "文章标题"
date: 2025-12-15T12:00:00+08:00
draft: false
tags: ['标签1', '标签2']
categories: ['分类']
author: '作者'
summary: '文章摘要'
---

文章内容...
```

### 代码高亮
```javascript
// 使用三个反引号包裹代码
console.log('Hello Hugo!');
```

## 🔧 自定义配置

### 修改博客信息
编辑 `blog/hugo.toml`：
```toml
[params]
  title = '你的博客标题'
  description = '博客描述'
  author = '作者名称'
```

### 自定义样式
编辑 `blog/layouts/` 目录下的HTML文件中的CSS部分。

### 修改输出目录
在 `blog/hugo.toml` 中修改：
```toml
publishDir = '../public/blog'
```

## 🚀 部署到生产环境

### 自动化部署
构建命令会自动处理博客：
```bash
npm run build
```

### 手动部署
1. 构建博客：`npm run blog:build`
2. 将 `public/blog/` 目录内容部署到服务器的 `blog/` 目录

## 📚 更多资源

- [Hugo官方文档](https://gohugo.io/documentation/)
- [Markdown语法指南](https://www.markdownguide.org/)
- [项目博客目录](blog/README.md)

## 💡 常见问题

**Q: 如何修改博客主题？**
A: 可以在 `blog/layouts/` 目录中修改HTML模板和CSS样式。

**Q: 如何添加自定义域名？**
A: 修改 `blog/hugo.toml` 中的 `baseURL` 配置。

**Q: 如何优化图片？**
A: 将图片放在 `blog/static/` 目录，Hugo会自动处理。

---

🎉 现在你可以开始使用你的Hugo博客系统了！